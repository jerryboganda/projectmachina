[package]
name = "machina-protocol-http"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Transport-neutral HTTP command adapter over the canonical command bus."

[dependencies]
machina-command-bus = { path = "../command-bus" }
machina-command-model = { path = "../command-model" }
machina-chromium-adapter = { path = "../chromium-adapter" }
machina-native-core = { path = "../native-core" }
serde_json = "1.0.151"
