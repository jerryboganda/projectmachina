# Canonical protobuf sources

M0-T04 owns the versioned command, event, error, and capability schemas. This
directory is reserved as the single protobuf source location; adapters must not
define divergent transport contracts.
