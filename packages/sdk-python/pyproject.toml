[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "project-machina-sdk"
version = "0.1.0a0"
description = "Async typed Project Machina client."
requires-python = ">=3.9"

[tool.setuptools.packages.find]
include = ["machina_sdk*"]
