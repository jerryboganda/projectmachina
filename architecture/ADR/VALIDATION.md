# ADR validation

An accepted ADR must have a decision owner, a status of `accepted` or
`superseded`, explicit consequences, and links to affected contracts and
evidence. Boundary changes also require generated output and compatibility
tests before merge.
