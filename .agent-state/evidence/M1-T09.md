# Evidence — M1-T09

## Identity

- Task: M1-T09 — Implement capability registry and router version zero
- Branch: `agent/M1-T09-capability-router`
- Worktree: `D:\Projects\machina-worktrees\M1-T09-capability-router`
- Base commit: `27ba1a29bdf466e7eea324d4f9e6b3d8a4d6b967`
- Pull request: to be opened from `agent/M1-T09-capability-router`

## Changed files

- `Cargo.lock`
- `crates/capability/Cargo.toml`
- `crates/capability/src/lib.rs`
- `crates/command-bus/Cargo.toml`
- `crates/command-bus/src/lib.rs`
- `.agent-state/evidence/M1-T09.md`

## Implemented

- Added versioned capability snapshots, policy-enabled status, stable capability
  responses, and a registry that replaces stale engine snapshots.
- Added explicit routing decision records containing policy, selected engine,
  reason, engine version, capability snapshot version, and fallback state.
- Added `CommandBus::execute_with_decision` and a capability registry projection
  sourced directly from runtime adapter snapshots.
- Kept unsupported, experimental, and disabled capabilities ineligible; no
  compatible engine produces a typed unsupported-capability failure.

## Focused validation

- `cargo fmt --all`: passed.
- `cargo test -p machina-capability -p machina-command-bus --tests --locked`: passed (9 tests).
- `cargo clippy -p machina-capability -p machina-command-bus --all-targets --locked -- -D warnings`: passed.
- `cargo check --workspace --locked`: passed.
- `git diff --check`: passed.

## Acceptance mapping

- Versioned snapshots/policy statuses: `CapabilitySnapshot`, `CapabilityRegistry`,
  `CapabilityResponse`, and policy override tests.
- Explicit routing policies: deterministic native/Chromium preference and fallback
  decisions with structured reason/version evidence.
- Safety and parity: disabled/no-capacity tests plus registry snapshots sourced
  from the same adapter capabilities used by routing.

## Deferred heavy validation

Multi-worker placement/load, real Chromium capability discovery, protocol
conformance, and M8/M9 certification suites remain scheduled for their owning
tasks. Docker-backed worker execution remains under the recorded M1 waiver.
