# Evidence — M0-T01

## Identity

- Task: M0-T01 — Initialize the polyglot monorepo and pinned toolchains
- Agent/tool: Copilot foundation implementation session
- Branch: `agent/M0-T01-bootstrap`
- Worktree: `D:\Projects\machina-worktrees\M0-T01-bootstrap`
- Base commit: `f62eeb7`
- Current commit: `HEAD` (verify with `git rev-parse HEAD`; last verified evidence refresh was `a506f19`)
- Owned scope: root bootstrap/build/CI/toolchain files and generated foundation outputs
- Pull request: https://github.com/jerryboganda/projectmachina/pull/1

## Objective and acceptance criteria

- Create the prescribed polyglot repository layout.
- Pin Rust, Node/pnpm, CMake/Clang, Buf, V8, and Chromium metadata.
- Provide doctor, bootstrap, build, check, format, test, and clean commands.
- Compile minimal Rust, C++ bridge, and SvelteKit workspace on a supported host.

## Implemented

- Root Rust workspace and pinned `rust-toolchain.toml`.
- CMake project and narrow opaque V8 bridge stub with documented ownership.
- Pnpm workspace, console shell, Node version pin, and toolchain manifest.
- `just` command surface and Node-backed build scripts.
- Repository layout placeholders, environment example, ignore rules, and local bootstrap documentation.

## Commands and results

- `node scripts/build/doctor.mjs`: passed with Node 20.18.0, pnpm 9.15.0, Rust 1.86.0, CMake 4.4.2, Ninja 1.13.2, Clang 21.1.6, and Buf 1.47.2.
- `pnpm install --frozen-lockfile --ignore-scripts`: passed.
- `pnpm contract:generate` and `pnpm contract:check`: passed.
- `cargo fmt --all -- --check`: passed.
- `cargo check --workspace --locked`: passed.
- `cargo clippy --workspace --all-targets -- -D warnings`: passed.
- `cargo test --workspace --locked`: passed.
- `cmake --preset release` and `cmake --build --preset release`: passed.
- `pnpm --filter @machina/console check`: passed.
- `pnpm --filter @machina/console build`: passed.
- `pnpm test`: passed (17 focused tests plus console test).
- `pnpm architecture:check`, security, supply-chain, traceability, and secret scans: passed.
- `node --test scripts/agent/claims.test.mjs scripts/security/scan-secrets.test.mjs`: passed.
- `node --test scripts/agent/claims.worktree.test.mjs`: passed with a real temporary Git worktree and branch.
- `git diff --check`: passed.
- Pinned Buf Linux/Windows release digests recorded and verified in hosted-CI source.
- Shared claim heartbeat renewed at 2026-08-09T01:29+05:00; claim record is in the Git common-dir store.
- Hosted CI run `31278881549` passed for the earlier pushed branch; the latest ownership/evidence commits require the subsequent PR run.

## Remaining

1. Obtain independent review on the isolated branch.
2. Run hosted CI and apply repository branch protection/labels.
3. Verify Docker/Compose health separately for M0-T11.
4. Merge through the protected `main` process.

## Risks

- Docker/Compose health is not evidenced on this host because Docker is unavailable.
- V8/Chromium acquisition remains metadata-only in the M0 stub and needs release-grade
  integrity/provenance before native runtime work.
