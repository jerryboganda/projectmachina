# Evidence — M0-T01

## Identity

- Task: M0-T01 — Initialize the polyglot monorepo and pinned toolchains
- Agent/tool: local implementation session
- Branch: local working tree; Git branch not yet initialized
- Worktree: `D:\Projects\Project Machina`
- Base commit: unavailable until Git initialization

## Objective and acceptance criteria

- Create the prescribed polyglot repository layout.
- Pin Rust, Node/pnpm, CMake/Clang, Buf, V8, and Chromium metadata.
- Provide doctor, bootstrap, build, check, format, test, and clean commands.
- Compile minimal Rust, C++ bridge, and SvelteKit workspace on a supported host.

## Implemented

- Root Rust workspace and pinned `rust-toolchain.toml`.
- CMake project and narrow opaque V8 bridge stub with documented ownership.
- Pnpm workspace, console shell, Node version pin, and toolchain manifest.
- `just` command surface and Node-backed build scripts.
- Repository layout placeholders, environment example, ignore rules, and local bootstrap documentation.

## Commands and results

- `pnpm bootstrap`: not run; terminal execution is unavailable in this session.
- `cargo check --workspace`: not run; terminal execution is unavailable in this session.
- `cmake -S . -B build/v8-bridge`: not run; terminal execution is unavailable in this session.
- `pnpm --filter @machina/console check`: not run; terminal execution is unavailable in this session.

## Remaining

1. Initialize Git and record the base/current commit.
2. Run the fast gate in a terminal-capable session.
3. Repair any compile, lockfile, formatting, or workflow findings.
4. Obtain independent review before committing and pushing through `main`.

## Risks

- The checked-in pnpm lockfile is a bootstrap placeholder and must be resolved by the pinned pnpm version before protected CI is enabled.
- Generated command-model projections must be refreshed by `pnpm contract:generate`.
