# Evidence — M1-T01

## Identity

- Task: M1-T01 — Implement control-plane database schema and durable event outbox
- Branch: `agent/M1-T01-control-plane`
- Worktree: `D:\Projects\machina-worktrees\M1-T01-control-plane`
- Base commit: `7db9d24`
- Current commit: `HEAD` (verify with `git rev-parse HEAD`)

## Implemented

- PostgreSQL up/down migrations for organizations, projects, policies, sessions,
  workers, artifacts, workflows, usage, audit records, and event outbox.
- Tenant/project foreign keys, optimistic versions, idempotency uniqueness,
  outbox aggregate-version uniqueness, indexes, and unpublished-event index.
- Dependency-free Rust control-plane repository contract and in-memory
  transaction model for fast tests.
- Authorization context carries actor ID, tenant scope, and policy hash; SQL
  child tables use composite organization/project ownership foreign keys.
- Atomic create/transition behavior, tenant-scoped reads, duplicate idempotency
  replay/rejection, and optimistic version conflicts.

## Focused validation

- `cargo fmt --all`: passed.
- `cargo check -p machina-control-plane --tests`: passed.
- `cargo test -p machina-control-plane --tests --locked`: passed (3 tests).
- `git diff --check`: passed.

## Remaining

- Hosted fast-gate and independent platform review.
- PostgreSQL migration up/down smoke when a database runtime is available.
- M1-T02 authentication/tenancy authorization depends on this merge.
