# Evidence — M1-T05

## Identity

- Task: M1-T05 — Build prewarmed Chromium worker pool and isolation controls
- Branch: `agent/M1-T05-workers`
- Worktree: `D:\Projects\machina-worktrees\M1-T05-workers`
- Base commit: `52b38d7`
- Current commit: `HEAD` (verify with `git rev-parse HEAD`)

## Implemented

- Explicit worker lifecycle and isolation-tier state.
- Runtime-unavailable prewarm failure that never reports a ready worker.
- Lease tokens, generation/reset counters, drain state, and stale release/reset
  rejection.
- Chromium service integration boundary documentation.

## Focused validation

- `cargo fmt --all`: passed.
- `cargo check -p machina-worker-pool --tests`: passed.
- `cargo test -p machina-worker-pool --tests --locked`: passed (2 tests).
- `git diff --check`: passed.

## Limitation

Actual Chromium process prewarming and container isolation remain runtime
integration work and are not claimed by this source-only slice under the owner
Docker waiver.
