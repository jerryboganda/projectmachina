# Evidence — M1-T03

## Identity

- Task: M1-T03 — Implement session lifecycle service and idempotent control API
- Branch: `agent/M1-T03-session`
- Worktree: `D:\Projects\machina-worktrees\M1-T03-session`
- Base commit: `e2a20c6`
- Current commit: `HEAD` (verify with `git rev-parse HEAD`)

## Implemented

- Typed session-control service over the M1-T01 repository contract.
- Create/get/cancel/close operations with authorization context and expected
  aggregate versions.
- Idempotent create replay and ordered closing/closed transitions.
- Validated lifecycle transition graph, full organization/project idempotency
  scope, authorization-scoped outbox reads, and terminal cancel/close replay.
- Focused cross-tenant negative behavior and lifecycle tests.

## Focused validation

- `cargo fmt --all`: passed.
- `cargo check -p machina-session-control --tests`: passed.
- `cargo test -p machina-session-control --tests --locked`: passed (2 tests),
  including terminal replay and invalid-transition coverage.
- Control-plane unit coverage includes cross-organization idempotency, scoped
  outbox reads, and invalid transition rejection.
- `git diff --check`: passed.

## Remaining

- Hosted fast-gate and independent platform review.
- HTTP/API adapter wiring is a later M1 task.
