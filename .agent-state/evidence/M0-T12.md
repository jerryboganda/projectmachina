# Evidence — M0-T12

## Implemented

- Deterministic local rehearsal for two non-overlapping claims.
- Overlap rejection, heartbeat renewal, handoff write/read, resume assertion,
  and release records.
- Rehearsal test is included in the foundation test command.

## Limitation

This is a file-backed local rehearsal. It does not claim the full M0 exit:
actual Git worktrees, pull requests, merge queue, independent reviewer, and
protected `main` still require a terminal-capable repository session.

## Validation

- `node --test scripts/agent/rehearsal.test.mjs`: not run in this session because
  terminal execution is unavailable.
