# Evidence — M1-T02

## Identity

- Task: M1-T02 — Implement authentication, tenancy, authorization and project policy
- Branch: `agent/M1-T02-auth`
- Worktree: `D:\Projects\machina-worktrees\M1-T02-auth`
- Base commit: `e2a20c6`
- Current commit: `HEAD` (verify with `git rev-parse HEAD`)

## Implemented

- Project-scoped credential metadata with SHA-256 token hashes and short
  non-secret prefixes; plaintext tokens are not retained.
- Revocation and invalid-token failure paths.
- Authorization context binding actor ID, project scope, and effective policy
  hash; cross-tenant access is denied.
- Stable effective-policy hash over canonical field ordering with invalid-policy
  rejection.
- PostgreSQL credential and policy-audit metadata migration with composite
  tenant ownership foreign keys.

## Focused validation

- `cargo fmt --all`: passed.
- `cargo check -p machina-auth -p machina-policy --tests`: passed.
- `cargo test -p machina-auth -p machina-policy --tests --locked`: passed (4 tests).
- `git diff --check`: passed.

## Remaining

- Hosted fast-gate and independent security/platform review.
- Credential API/rotation integration in the M1 control service.
