# Evidence — M1-T04

## Identity

- Task: M1-T04 — Implement scheduler, worker registry, leases and fair queue
- Branch: `agent/M1-T04-scheduler`
- Worktree: `D:\Projects\machina-worktrees\M1-T04-scheduler`
- Base commit: `bd833ac`
- Current commit: `HEAD` (verify with `git rev-parse HEAD`)

## Implemented

- Tenant-rotating fair queue with priority/FIFO ordering within each tenant.
- Worker registry with explicit available/busy/draining/offline state model.
- Acquire, renew, release, and expiry-aware worker leases.
- Duplicate worker/item and expired/unavailable failure paths.

## Focused validation

- `cargo fmt --all`: passed.
- `cargo check -p machina-scheduler --tests`: passed.
- `cargo test -p machina-scheduler --tests --locked`: passed (2 tests).
- `git diff --check`: passed.
