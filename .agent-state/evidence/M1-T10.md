# Evidence — M1-T10

## Identity

- Task: M1-T10 — Implement traces, classified artifacts and reproduction bundle version zero
- Branch: `agent/M1-T10-traces-artifacts`
- Worktree: `D:\Projects\machina-worktrees\M1-T10-traces-artifacts`
- Base commit: `1df75b78e6fab36b8f41c09681565e46178370c5`
- Pull request: to be opened from `agent/M1-T10-traces-artifacts`

## Changed files

- `Cargo.toml`
- `Cargo.lock`
- `crates/telemetry/Cargo.toml`
- `crates/telemetry/src/lib.rs`
- `crates/command-bus/Cargo.toml`
- `crates/command-bus/src/lib.rs`
- `crates/protocol-http/Cargo.toml`
- `crates/protocol-http/src/lib.rs`
- `crates/artifact-store/Cargo.toml`
- `crates/artifact-store/src/lib.rs`
- `migrations/005_artifact_access.up.sql`
- `migrations/005_artifact_access.down.sql`
- `services/artifact-store/README.md`
- `.agent-state/evidence/M1-T10.md`

## Implemented

- Added bounded ordered `SessionTrace` correlation events spanning request,
  worker outcome, and artifact generation without storing raw page content.
- Added tenant-scoped classified artifact metadata and caller-supplied ciphertext
  storage with SHA-256 integrity verification and key references.
- Added HMAC-signed, project-scoped, short-lived artifact grants capped by object
  expiry, with cross-tenant, expiry, and tamper rejection.
- Added redacted reproduction bundles containing trace events and artifact
  metadata/checksums but never object bytes; bundle hashes and round trips verify.
- Added canary/sensitive metadata rejection and durable artifact access grant
  schema/indexing.

## Focused validation

- `cargo fmt --all`: passed.
- `cargo test -p machina-command-bus -p machina-protocol-http -p machina-telemetry -p machina-artifact-store --tests`: passed (15 tests).
- `cargo clippy -p machina-command-bus -p machina-protocol-http -p machina-telemetry -p machina-artifact-store --all-targets -- -D warnings`: passed.
- `cargo check --workspace`: passed.
- `git diff --check`: passed.

## Acceptance mapping

- Session trace correlation: bounded monotonic `SessionTrace` with shared
  `CorrelationContext` and ordered request/outcome/artifact events.
- Bundle integrity/redaction: `ReproductionBundle::validate`, canonical checksum,
  round-trip test, ciphertext exclusion, and canary rejection.
- Tenant artifact access: exact scope checks, hash verification, signed expiry,
  tamper rejection, and cross-tenant tests.

## Deferred heavy validation

PostgreSQL execution, real encrypted object storage/KMS, distributed trace
export, cross-process signed URL serving, load, soak, chaos, and M8/M9
certification remain scheduled for their owning tasks. Docker-backed runtime
evidence remains under the recorded M1 waiver.
