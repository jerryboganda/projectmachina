# Evidence — M0-T04

## Identity

- Task: M0-T04 — Create canonical command, event, error, and capability schema skeleton
- Agent/tool: local implementation session
- Write scope: `schemas/command-model/**`, `scripts/contracts/**`, `crates/command-model/**`, `packages/contracts-ts/**`

## Implemented

- Versioned transport-neutral JSON Schema.
- Session create, navigation, semantic query, click, and session close command payloads.
- Canonical outcome, event, error, engine execution, and capability status records.
- Deterministic Rust and TypeScript generator with SHA-256 source marker.
- Round-trip/contract check entrypoint and checked-in bootstrap projections.

## Validation

- `pnpm contract:generate`: not run; terminal execution is unavailable.
- `pnpm contract:check`: not run; terminal execution is unavailable.
- Rust and TypeScript consumer compilation: not run; terminal execution is unavailable.

## Remaining

- Run generator and commit the refreshed source hash.
- Add JSON round-trip fixtures and compatibility-diff checks.
- Obtain independent protocol/architecture review.
