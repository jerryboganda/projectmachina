# Evidence — M0-T04

## Identity

- Task: M0-T04 — Create canonical command, event, error, and capability schema skeleton
- Agent/tool: copilot-contracts bounded implementation session
- Branch: `agent/M0-T04-contracts`
- Worktree: `D:\Projects\machina-worktrees\M0-T04-contracts`
- Base commit: `0088d3e`
- Current commit: `HEAD` (verify with `git rev-parse HEAD` after commit)
- Write scope: `schemas/command-model/**`, `scripts/contracts/**`, `crates/command-model/**`, `packages/contracts-ts/**`

## Implemented

- Versioned transport-neutral JSON Schema.
- Session create, navigation, semantic query, click, and session close command payloads.
- Canonical outcome, event, error, engine execution, and capability status records.
- Deterministic Rust and TypeScript generator with SHA-256 source marker.
- Round-trip/contract check entrypoint and checked-in bootstrap projections.

## Validation

- `node scripts/contracts/generate.mjs`: passed.
- `node scripts/contracts/check.mjs`: passed.
- `node scripts/contracts/roundtrip.mjs`: passed.
- `node scripts/contracts/typecheck.mjs`: passed.
- `cargo check -p machina-command-model --tests --locked`: passed.
- `git diff --check`: passed.

## Remaining

- Commit the refreshed source hash, serde-backed Rust bindings, and contract fixtures.
- Obtain independent protocol/architecture review.
