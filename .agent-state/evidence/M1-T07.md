# Evidence — M1-T07

## Identity

- Task: M1-T07 — Expose initial HTTP, gRPC and event-stream adapters
- Branch: `agent/M1-T07-protocols`
- Worktree: `D:\Projects\machina-worktrees\M1-T07-protocols`
- Base commit: `f3f8862`
- Current commit: `HEAD` (verify with `git rev-parse HEAD`)

## Implemented

- Ordered resumable event stream with bounded backpressure and acknowledgements.
- Transport-neutral HTTP command adapter over the single command bus.
- Explicit 404/200/501 mappings and canonical typed errors.
- Chromium-unavailable behavior remains a typed failure.

## Focused validation

- `cargo fmt --all`: passed.
- `cargo check -p machina-protocol-events -p machina-protocol-http --tests --locked`: passed.
- `cargo test -p machina-protocol-events -p machina-protocol-http --tests --locked`: passed (3 tests).
- `git diff --check`: passed.

## Deferred

Actual HTTP/gRPC listener/runtime deployment and full client conformance are
owned by subsequent protocol tasks.
