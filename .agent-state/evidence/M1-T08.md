# Evidence — M1-T08

## Identity

- Task: M1-T08 — Implement ordered events, idempotent delivery and backpressure
- Branch: `agent/M1-T08-events`
- Worktree: `D:\Projects\machina-worktrees\M1-T08-events`
- Base commit: `f39f5802f4913d207903686dca066eb9e539e1f4`

## Implemented

- Added bounded per-session `EventBroker` history and per-subscriber queues.
- Added monotonic session sequences, acknowledgement, reconnect replay, explicit
  `ResyncRequired`, and a reset path after client snapshot resynchronization.
- Added stable stream event IDs and idempotent consumers that mark delivery only
  after a successful effect.
- Added control-plane sequence-aware outbox replay and an atomic in-memory
  transactional projection contract.
- Added PostgreSQL event sequence migration, durable delivery ledger, and
  `machina_begin_event_delivery` transaction helper.
- Normalized lifecycle event state payloads to canonical lowercase values.

## Focused validation

- `cargo fmt --all`: passed.
- `cargo test -p machina-protocol-events -p machina-control-plane --tests --locked`: passed (12 tests).
- `cargo clippy -p machina-protocol-events -p machina-control-plane --all-targets --locked -- -D warnings`: passed.
- `cargo check --workspace --locked`: passed.
- `git diff --check`: passed.

## Acceptance mapping

- Ordered per-session events and reconnect gaps: `EventBroker`, `outbox_for_after`,
  `event_outbox.sequence`, and replay tests.
- Slow-reader bounds and resync: independent subscriber capacity, overflow marker,
  `ResyncRequired`, `reset_subscriber`, and slow-reader tests.
- Idempotent durable effects: `EventDeliveryLedger`, transactional in-memory
  projection test, SQL delivery ledger/function, and transaction documentation.

## Deferred heavy validation

PostgreSQL migration execution, multi-process transport reconnect, full protocol
conformance, load, soak, chaos, and certification suites remain scheduled for the
corresponding M8/M9 tasks. Docker/Compose runtime execution remains under the
recorded M1 owner waiver.
