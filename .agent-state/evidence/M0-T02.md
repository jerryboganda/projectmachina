# Evidence — M0-T02

## Identity

- Task: M0-T02 — Implement task claims, leases, worktree and evidence helpers
- Agent/tool: copilot-claims bounded implementation session
- Branch: `agent/M0-T02-claims`
- Worktree: `D:\Projects\machina-worktrees\M0-T02-claims`
- Base commit: `0088d3ec8ba585f01fe97654435853e252e21651`
- Current commit: `HEAD` (verify with `git rev-parse HEAD` after commit)
- Write scope: `scripts/agent/**`, `.agent-state/**`

## Completed

- Shared claim storage resolves Git worktrees to the Git common directory and
  retains a non-Git temporary-root fallback for isolated tests.
- Scope paths are canonicalized as repository-relative globs with conservative,
  path-boundary-aware overlap detection.
- Claims bind task, agent, branch, worktree, owner token, lease, heartbeat,
  dependencies, and write scopes. Heartbeat and release require the owner
  identity; expiry cannot be revived.
- Claim and coordination locks use atomic directory acquisition, explicit stale
  recovery, audit records, and fencing tokens.
- Task registry loading validates task IDs, dependency declarations, unknown
  dependencies, cycles, and available durable completion status.
- `worktree.mjs` creates, inspects, and removes registered Git worktrees and
  branches without mutating protected default branches or silently discarding
  dirty work.
- `handoff.mjs` and `evidence.mjs` project machine-readable and Markdown
  handoff/evidence records under `.agent-state/evidence/`.

## Acceptance mapping

- Overlap rejection: `claims.test.mjs`; active claims with intersecting scopes
  fail deterministically.
- Lease expiry and owner fencing: `claims.test.mjs` and
  `claims.recovery.test.mjs`; expired claims require explicit recovery and
  stale locks produce a fencing audit.
- Two-worktree persistence: `claims.worktree.test.mjs` and
  `worktree.test.mjs`; claims use the Git common store and helpers exercise
  real Git worktree lifecycle operations.
- Durable state/handoff: `handoff.test.mjs`; JSON and Markdown projections are
  written and reloadable.

## Commands and results

- `node --check scripts/agent/claims.mjs scripts/agent/task-registry.mjs scripts/agent/worktree.mjs scripts/agent/handoff.mjs scripts/agent/evidence.mjs`: passed.
- Focused claims/recovery/worktree/task-registry/handoff tests: passed (12 tests).
- `git diff --check`: passed.

## Changed files

- `scripts/agent/claims.mjs`
- `scripts/agent/claims.test.mjs`
- `scripts/agent/claims.worktree.test.mjs`
- `scripts/agent/claims.recovery.test.mjs`
- `scripts/agent/task-registry.mjs`
- `scripts/agent/task-registry.test.mjs`
- `scripts/agent/worktree.mjs`
- `scripts/agent/worktree.test.mjs`
- `scripts/agent/handoff.mjs`
- `scripts/agent/handoff.test.mjs`
- `scripts/agent/evidence.mjs`
- `scripts/agent/README.md`
- `.agent-state/README.md`
- `.agent-state/evidence/M0-T02.md`
- `.agent-state/evidence/M0-T02.handoff.md`
- `.agent-state/evidence/M0-T02.handoff.json`

## Failures and blockers

- Hosted CI and independent review remain pending.

## Remaining steps

1. Inspect the Git diff and verify no paths outside the claimed scope changed.
2. Commit with a message containing `M0-T02`, then push the branch.
3. Request independent review before merge; do not release the claim before
   review and merge state are recorded.

## Recommended next action

Enable terminal/Git tooling, reproduce the focused gate, repair any concrete
syntax/test findings, then commit the reviewed diff as `M0-T02`.
