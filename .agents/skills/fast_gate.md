---
title: Run the Fast Gate
description: Project Machina skill: run the fast gate.
---

# Skill: Run the Fast Gate

Run formatting/static validation, changed-package compilation or type checking, changed unit/contract tests, one focused deterministic behavior path, and immediate high-risk checks for unsafe/FFI, parser, auth, sandbox, secrets, egress, or migrations. Record exact commands, versions, outputs, durations, and artifacts.
